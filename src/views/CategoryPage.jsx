const React = require('react');
const Layout = require('./Layout');
module.exports = function CategoryPage({title, user, category, subcategories, courses}){
    return(
        <Layout title={title} user={user}>

        </Layout>
    )
}
